# Responses API Migration Plan for Presentation AI

## Overview
Migrate the ALLWEONE Presentation AI application from Chat Completions API to OpenAI's newer Responses API to unlock enhanced features and better performance.

## Current State Analysis
The application currently uses OpenAI SDK with Chat Completions for:
- AI-powered content generation
- Image generation (DALL-E)
- Text-to-diagram generation
- Presentation outline creation

## Features to Add/Enhance with Responses API

### 1. Native Web Search Tool
**Current**: Uses Tavily API (optional, separate service)
**Enhancement**: Migrate to OpenAI's built-in `web_search` tool
- Remove dependency on Tavily API
- Better integration with content generation
- Real-time search results within AI responses
- Cost savings (no separate API key needed)

**Files affected**:
- `src/ai/tools/search.ts`

### 2. Integrated Image Generation
**Current**: Separate DALL-E API calls
**Enhancement**: Use `image_generation` tool within responses
- Generate images inline during content creation
- Better context-aware image generation
- Single API call for both text and images

**Files affected**:
- `src/app/api/presentation/generate-image-slides/`
- `src/components/presentation/editor/`

### 3. Enhanced Multi-turn Outline Generation
**Current**: Separate API calls for outline and content
**Enhancement**: Use `previous_response_id` for conversation chaining
- Maintain context between outline and slide generation
- Better consistency across presentation
- Simpler state management

**Files affected**:
- `src/app/api/presentation/outline/`
- `src/app/api/presentation/generate/`

### 4. Code Interpreter for Chart Generation
**Current**: Basic chart components
**Enhancement**: Use `code_interpreter` tool to generate charts/diagrams
- Dynamic chart generation from data
- Python/R code execution for complex visualizations
- Export charts to presentation slides

**Files affected**:
- `src/ai/tools/presentation/tools.ts`
- `src/components/presentation/slides/`

### 5. Reasoning Models Integration
**Current**: Uses GPT-4/4o models
**Enhancement**: Use GPT-5 with improved reasoning
- Higher quality content generation
- Better structured output
- 3% improvement in accuracy (per OpenAI docs)

**Files affected**:
- Model selection throughout the app
- `src/ai/lib/model-picker.ts`

### 6. Stateful Conversation Memory
**Current**: Manual session management in database
**Enhancement**: Use `store: true` for native state management
- Reduced database operations
- Better conversation continuity
- Automatic context preservation

**Files affected**:
- `src/app/_actions/notebook/presentation/`
- Session management code

### 7. Streaming Response Updates
**Current**: Non-streaming responses with progress indicators
**Enhancement**: Use Responses API streaming for real-time generation
- Users see tokens appear as they're generated
- Live slide content building
- Progress streaming for better UX

**Files affected**:
- `src/app/api/presentation/generate/route.ts`
- `src/app/api/presentation/outline/route.ts`
- `src/states/presentation-state.ts`
- Frontend components for streaming display

### 8. Parallel Tool Execution
**Current**: Sequential API calls for different features
**Enhancement**: Use Responses built-in parallel tool execution
- Web search + image generation in single request
- Faster overall generation time
- Coordinated outputs

**Files affected**:
- All presentation generation API endpoints

## Implementation Steps

### Phase 1: Core Migration
1. Update `src/env.js` to add `OPENAI_RESPONSES_API_KEY` (same key works for both APIs)
2. Create `src/lib/openai/responses.ts` wrapper for Responses API
3. Update API routes to use Responses API format

### Phase 2: Web Search Integration
1. Replace Tavily search tool with native `web_search` tool
2. Update presentation generation to use integrated search
3. Remove Tavily API dependency (optional)

### Phase 3: Enhanced Features
1. Add inline image generation via `image_generation` tool
2. Implement code interpreter for charts
3. Add conversation state management with `store: true`

### Phase 4: Model Updates
1. Add GPT-5 model support
2. Update model selection UI
3. Configure reasoning effort levels

### Phase 5: Streaming & Parallel Tools
1. Implement streaming response handler
2. Add SSE (Server-Sent Events) endpoint for live updates
3. Enable parallel tool execution in presentation generation
4. Frontend streaming display components

## Streaming Implementation Details

### Backend Streaming Endpoint
Create `src/lib/openai/stream-handler.ts`:
- Transform Responses API stream to SSE format
- Send incremental updates for slide content
- Handle tool call streaming events

### Frontend Streaming Display
Update presentation state to:
- Receive stream events via EventSource
- Update slide content in real-time
- Show typing indicators for each section

### Files for Streaming
- `src/app/api/presentation/stream/route.ts` (new)
- `src/hooks/presentation/useStreamPresentation.ts` (new)
- `src/states/presentation-state.ts` (update)

## Benefits
- **Lower costs**: 40-80% better cache utilization
- **Better performance**: Improved reasoning models
- **Simplified architecture**: Fewer external dependencies
- **Native tools**: No need for custom function implementations
- **Future-proof**: Ready for upcoming OpenAI features

## Risks & Considerations
- Breaking changes in function/tool format
- Need to update structured output handling (`text.format` vs `response_format`)
- Backward compatibility for existing presentations
- Testing required for all API endpoints

## Estimated Effort
- Phase 1: 2-3 hours
- Phase 2: 1-2 hours  
- Phase 3: 3-4 hours
- Phase 4: 1-2 hours
- Phase 5 (Streaming): 2-3 hours
- Total: ~1-2 days for full migration

## Integration with openai-responses-starter-app Features

### Key Features to Adopt:

#### 1. **Persistent Conversations** 
- Add `responseId` to presentation-state.ts for tracking conversation chains
- Enable "continue building this deck" workflow
- Store previous_response_id for slide refinement

#### 2. **Streaming Memory**
- Implement SSE streaming for live slide generation
- Stream tool calls (web search, image generation) with progress indicators
- Show "researching topic" → "generating slide 1" → "creating visuals" in real-time

#### 3. **Tool Orchestration Functions**
Create presentation-specific functions:
- `generateSlide(outlineItem, theme, style)` 
- `generateThemeFromPrompt(prompt)`
- `generateSpeakerNotes(slideContent)`
- `generateChart(data, type)`
- `generateInfographic(topic)`

#### 4. **Vector Store Integration**
- File upload for PDF/DOC → presentation conversion
- Company knowledge base → branded decks
- Notion/GitHub integration via MCP for content sourcing

#### 5. **MCP Server Integration**
Priority integrations:
- SmartCRM (your MuAPI/Zep stack) → customer-specific decks
- Google Calendar/Gmail → automatic meeting presentations
- GitHub → documentation to slides

#### 6. **Display Annotations**
- Inline citations for statistics
- Source attribution on generated slides
- Confidence levels for AI-generated content

### Implementation Additions to Plan:

#### Phase 5: MCP Integration
1. Add MCP client configuration
2. Connect SmartCRM for CRM-aware presentations
3. Add Gmail/Calendar integration for meeting decks

#### Phase 6: Vector Store & File Upload
1. Enable document upload processing
2. Create vector stores for presentation context
3. Build RAG workflows for custom content

#### Phase 7: Presentation-to-Video
1. Integrate with Remotion for video export
2. Add AI narration pipeline
3. Create video scene generation from slides

## Integration Architecture Pattern

### From openai-responses-starter-app Repo:

#### **Conversation Store Pattern**
```javascript
// Similar to their conversation store, add to presentation-state.ts
interface ResponseChain {
  id: string;           // response_id from OpenAI
  previousId?: string;   // previous_response_id for chaining
  toolCalls: ToolCall[]; // executed tools in this response
  content: any;          // generated content
}
```

#### **Streaming Handler Pattern**
```typescript
// Create src/lib/openai/streaming.ts
export async function* streamPresentation(
  prompt: string,
  context: ResponseChain[]
): AsyncGenerator<StreamEvent> {
  // Use response.stream() from OpenAI SDK
  // Yield progress events for frontend
}
```

#### **Tool Orchestration Pattern**
```typescript
// Functions the AI can call during generation
const tools = [
  {
    type: "function",
    name: "generateSlide",
    parameters: { /* slide params */ }
  },
  {
    type: "web_search",  // native tool
  },
  {
    type: "image_generation",  // native tool
  }
];
```

#### **MCP Integration Pattern**
Add to `src/lib/mcp/clients.ts`:
- Connect to external MCP servers (SmartCRM, Calendar, Gmail)
- Expose presentation tools to MCP
- Enable bidirectional tool calling

## Questions for Implementation Priority

1. **Streaming Priority**: Should we implement SSE streaming first for live UX, or focus on backend migration?

2. **MCP Priority**: Which integration first - SmartCRM for customer decks, or Google Calendar for meeting prep?

3. **Vector Stores**: Should we enable document upload (PDF/DOC to slides) in phase 1 or later?

4. **Video Export**: Remotion integration for presentation-to-video - now or after core AI features?

---

## Summary of Integration Approach

The openai-responses-starter-app provides the **infrastructure patterns** we need. Instead of cloning the repo (which would cause conflicts), we'll **adapt its patterns**:

| Pattern | Our Implementation |
|---------|-------------------|
| Conversation Store | Extend `presentation-state.ts` with `responseId` tracking |
| Streaming Handler | Create `src/lib/openai/streaming.ts` with SSE support |
| Tool Orchestration | Define presentation tools in function format |
| MCP Client | Add `src/lib/mcp/` directory for server connections |
| Vector Store | Use existing Supabase storage + OpenAI file API |

This maintains our existing codebase while gaining all the advanced features.

## Detailed Integration Steps (From openai-responses-starter-app Patterns)

### Step 1: Create Responses API Wrapper
Create `src/lib/openai/responses.ts`:
- Initialize OpenAI client with `apiKey: process.env.OPENAI_API_KEY`
- Wrap `client.responses.create()` with our presentation context
- Add support for `stream: true` for live updates

### Step 2: Add AI Response Tracking to State
Add to `src/states/presentation-state.ts`:
- `aiResponseChain: ResponseChain[]` - track conversation history
- `currentResponseId: string | null` - track active response
- Methods: `addResponseToChain()`, `getPreviousResponseId()`

### Step 3: Create Streaming Endpoint
Create `src/app/api/presentation/stream/route.ts`:
- POST endpoint accepting `{ prompt, previous_response_id?, presentationId }`
- Use `response.stream()` from OpenAI SDK
- Transform chunks to SSE format with event types:
  - `content.delta` - streaming text
  - `tool.call.start` - tool started
  - `tool.call.complete` - tool finished
  - `response.complete` - final output

### Step 4: Create Presentation Tools
Create `src/lib/openai/presentation-tools.ts`:
- `generateSlide`: Creates slide with content, layout, styling
- `generateTheme`: Builds custom theme from text prompt
- `generateSpeakerNotes`: Creates presenter notes for each slide
- `generateChart`: Uses code_interpreter for data visualization
- `searchWeb`: Native web search tool

### Step 5: MCP Client Integration
Create `src/lib/mcp/`:
- `client.ts` - Base MCP client class
- `smart-crm.ts` - Connect to MuAPI/Zep MCP server
- `calendar.ts` - Google Calendar integration
- `gmail.ts` - Gmail integration

### Step 6: Vector Store Integration
Create `src/lib/openai/vector-store.ts`:
- `createVectorStore(name)` - create stores for presentations
- `uploadFile(file)` - handles PDF/DOC/TXT uploads
- `addFileToVectorStore(storeId, fileId)` - associate files
- `searchVectorStore(storeId, query)` - semantic search